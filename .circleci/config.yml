version: 2
jobs:
  build:
    docker:
      - image: circleci/node:10
    steps:
      - add_ssh_keys:
          fingerprints:
            -
      - checkout
      - run:
          name: Install
          command: npm Install
      - run:
          name: build
          command: npm build
